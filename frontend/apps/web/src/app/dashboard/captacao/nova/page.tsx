'use client';

import React from 'react';
import { PageEditorWorkspace } from '@/components/landing-builder/PageEditorWorkspace';

export default function NewCapturePage() {
  return <PageEditorWorkspace isNewPage={true} />;
}
